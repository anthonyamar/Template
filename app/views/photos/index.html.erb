<div class="container">
  <div class="heading mb-5">
    <h1><%= @tinder_profile.user.name %></h1><br>
    <p><%= @tinder_profile.user.bio %></p>
    <%= link_to "<- Swipe left", '#', class: "btn btn-danger" %>
    <%= link_to "Swipe right ->", '#', class: "btn btn-success" %>
  </div>
  <div class="row">
    <% @tinder_profile.user.photos.each do |photo| %>
      <div class="card mr-3 mb-3" style="width: 18rem;">
        <img class="card-img-top" src="<%= photo.url %>">
        <div class="card-body">
          <div class="row">
            <div class="col-sm-6">
              <a href="#" class="btn btn-warning btn-block mt-2" onclick="pass(<%= photo.url %>);">
                Pass
              </a>
            </div>
            <div class="col-sm-6">
              <a href="#" class="btn btn-info btn-block mt-2" onclick="like(<%= photo.url %>);">
                Like
              </a>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>


<script>

  var like = function(url) {

  }

  var pass = function(url) {

  }

</script>